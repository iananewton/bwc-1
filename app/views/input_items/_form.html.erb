<%= form_for(@input_item) do |f| %>
  <% if @input_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@input_item.errors.count, "error") %> prohibited this input_item from being saved:</h2>

      <ul>
      <% @input_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :InputType %><br>
    <%= f.number_field :InputType %>
  </div>
  <div class="field">
    <%= f.label :InputItemName %><br>
    <%= f.text_area :InputItemName %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
