<%= form_for(@vessel_type) do |f| %>
  <% if @vessel_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vessel_type.errors.count, "error") %> prohibited this vessel_type from being saved:</h2>

      <ul>
      <% @vessel_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :VesselTypeName %><br>
    <%= f.text_area :VesselTypeName %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
