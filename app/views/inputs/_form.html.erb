<%= form_for(@input) do |f| %>
  <% if @input.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@input.errors.count, "error") %> prohibited this input from being saved:</h2>

      <ul>
      <% @input.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :MasterDataID %><br>
    <%= f.number_field :MasterDataID %>
  </div>
  <div class="field">
    <%= f.label :InputItem %><br>
    <%= f.number_field :InputItem %>
  </div>
  <div class="field">
    <%= f.label :Quantity %><br>
    <%= f.text_field :Quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
